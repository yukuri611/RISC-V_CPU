# ==========================================
# JAL / JALR 動作確認テスト
# ==========================================

# --- 初期化 ---
addi x10, x0, 0     
addi x1,  x0, 0     
jal x1, func_add     
addi x10, x10, 100   # 成功フラグ: x10 に 100 を足す
                     # 期待値: x10 = 5 + 100 = 105 (0x69)

end_loop:
    beq x0, x0, end_loop


func_add:
    addi x10, x10, 5    # 計算処理: x10 = 0 + 5
    jalr x0, x1, 0
    
    # --- ここは実行されてはいけない ---
    addi x10, x10, 99
